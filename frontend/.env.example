# Backend API URL. Set this when running the frontend against a hosted backend.
# Default: http://localhost:8000 (local dev)
# For hosted Docker: set to the public URL of the backend container
VITE_API_URL=http://localhost:8000
